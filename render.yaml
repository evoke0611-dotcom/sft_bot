services:
  - type: web
    name: sft_whats_bot
    env: python
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn src.whatsapp_bot:app --host 0.0.0.0 --port $PORT
      - key: PYTHON_VERSION
        value: 3.10.11